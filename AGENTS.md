# AGENTS.md

「追いつかれた車両の義務」の有無が渋滞のできかたに与える影響を、L区間（義務あり）と R区間（義務なし）の 2 本を並べて比較する Three.js 製の渋滞シミュレーション。

---

## A. 最重要: 実験的妥当性の不変条件

**このプロジェクトの存在意義に直結する。何よりも先に守ること。**

- 本シミュレーションは **L区間（義務あり）** と **R区間（義務なし）** を並置し、「**追いつかれた車両の義務**」というルール 1 つの有無だけが渋滞に与える影響を比較する対照実験である。
- **両区間の挙動差は、この「追いつかれた車両の義務」ただ 1 つでなければならない。** それ以外に区間で異なる挙動を導入すると交絡（confounding）が生じ、比較そのものが無意味になる。
- **実装上の禁止事項:** 新しい挙動の分岐条件に、区間で値が変わる変数（`section` / `yields` / `camper` / `keepLeft` / `returnTime` など）を使ってはならない。唯一の例外は既存の「追いつかれた車両の義務」ロジック（`src/core/vehicle.ts` の `if (this.yields && flowing && this.lane < 2)` の箇所とキープレフト）である。新しい挙動は **L/R 完全同一（区間非依存）** で実装する。物理（加速・車間・合流など）は当然両区間共通。
- **回帰ガード:** `traffic-simulation.test.ts` のシードテストが L/R の渋滞スコア差「**約 4 ± 1**」（`DIFF_TARGET = 4`、10 シード平均）を強制する。これを壊さないこと。そして **その数値を回復するために区間依存の条件を足して辻褄を合わせる行為は禁止**。それはこの不変条件そのものの違反であり、本末転倒である。数値が区間非依存で回復できないなら、**実装を止めて人間に報告する**。
- **なぜ 10 から 4 になったのか（再較正の経緯）:** 旧値「約 10」は、実在のバグ 2 件が差を水増ししていた状態で較正されたものだった。
  - **#50**: `checkLaneSafetyForChange` が周回（ラップ）を考慮せず素の z 比較をしていたため、継ぎ目付近で「遠く離れている」と誤認して危険な車線変更が成立していた（main には実車両の重なり −1.573m が実在した）。main の成立済み車線変更を周回換算で再判定すると **L 8.64% / R 13.21%** がこのバグに依存しており、**恩恵が R に 1.53 倍偏っていた**。混雑する R ほど継ぎ目付近の車線変更が多いためで、これは創発的な非対称であって構造的な交絡ではない（修正コードは `section` 等の区間変数を一切読まない）。
  - **#52**: `sectionIndex` の陳腐化で `findAhead` が最近接の前方車を見落としていた。
  - 修正後の実測は 10 シード平均 **4.00**（シード別 1.25〜9.19）。**10 シード全てで R > L は維持**されており、「義務なし区間のほうが渋滞する」という本実験の結論は覆っていない。変わったのは較正された差の**大きさ**だけである。
  - 再較正では**調整パラメータ（`requiredGap` 係数や `src/core/constants.ts` の物理定数）を一切触っていない**。期待値の側を実測に合わせた。
- **台数指標について:** 「混雑側に車両が滞留して平均台数が多くなる」は**もはや成り立たない**。`admissibleLane()` が `roadCount >= targetCountPerSection()` で本線への進入を止めるため、両区間は同じ台数水準に能動的に regulate され、台数指標は飽和する（実測の平均台数差は +0.05 台、入口待ち差は −0.25 台で符号すら逆）。L/R の差は**スコアの速度項（重み 75%）と流出台数**に現れる。台数で差を出そうとしないこと。
- **経緯（なぜこの規約があるか）:** かつて合流協調を `yields` でゲートしたところ、「義務」が合流にも二重に効いて交絡になり、レビューで是正された実例がある。区間差を生む変数に安易に相乗りしないこと。

---

## B. 車線・座標の規約

- **lane index の意味:**
  - `0` = 追い越し車線（進行方向の**右端**, `+X` 側）
  - `1〜2` = 走行車線
  - `3` = 加速車線（合流ランプ）
  - index が大きいほど外（左）側になる。実値は `src/core/constants.ts` の `LANE_X` 参照。
- **R区間は L区間の鏡像ではなく平行移動コピー**（`SECTION_OFFSET_X`）。これにより追い越し車線は両区間とも右、**両区間の合流条件まで完全に同一**になる。鏡像にすると R だけ追い越し車線が左になり交絡するので不可。
- **進行方向は `-Z`**（前方 = `z` が小さい側）。合流ランプ（`RAMP_Z_TOP` → `RAMP_Z_END`）も z が減る向きに走る。座標定数は `src/core/constants.ts` 参照。

---

## C. 開発・検証コマンド

- ランタイムは **Node.js**（版数は `.node-version` / `package.json#devEngines` 参照）。
- パッケージマネージャは **pnpm**（版数は `package.json#packageManager` 参照）。
- ツールチェーンは **Vite+（`vp`）** に統一（ビルド=Vite / テスト=Vitest / lint=Oxlint / フォーマット=Oxfmt / 型チェック）。設定は `vite.config.ts` に集約。

| コマンド                    | 内容                                                                                   |
| --------------------------- | -------------------------------------------------------------------------------------- |
| `pnpm install`              | 依存導入 + git hooks 導入（`prepare` で `vp config` が走る）                           |
| `pnpm check`                | **フォーマット + lint + 型チェックを一括実行**（`vp check`）。コミット前の基本チェック |
| `pnpm test`                 | シミュレーションコアのテスト（Vitest, `vp test run`）                                  |
| `pnpm build`                | `dist/` へバンドル出力                                                                 |
| `pnpm lint` / `pnpm fmt`    | lint のみ / フォーマット適用                                                           |
| `pnpm dev` / `pnpm preview` | 開発サーバー / ビルド結果確認サーバー                                                  |

- 依存（three / lucide）は pnpm で管理し Vite+ でバンドルする。**CDN は使わない。**
- コミット時は `.vite-hooks` の pre-commit フックが `vp staged`（= ステージ済みへ `vp fmt`）を適用する。

---

## D. テスト規約

- テストは単一ファイル **`traffic-simulation.test.ts`**（`src/core/` の DOM/THREE 非依存なシミュレーションロジックを検証）。
- 構成は**内容ごとの `describe` セクション**（例: `追いつかれた車両の義務`、`追い越し`、`流入・流出と滞留 (Issue #12)` など。Issue 由来のものは末尾に `(Issue #N)` を付ける運用）。
- **新規テストはファイル末尾に、既存セクションと重複しない新しい `describe` ブロックとして追加する。** 並行作業でファイル末尾が衝突しやすいので、追加前に既存の `describe` 一覧を確認すること。
- スコア差の回帰ガード（A 参照）を含むため、テストの期待値を都合よく緩めないこと。

---

## E. コード・コミット規約

- **コメント・コミットメッセージは日本語**で書く。
